# Frontend Angular 18

La documentación de instalación y ejecución está en el [README del repositorio](../README.md).

```powershell
npm install
npm start
```

La API se configura en `src/environments/environment.development.ts`.
